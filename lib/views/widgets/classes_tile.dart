import 'package:flutter/material.dart';

class ClassesTile extends StatelessWidget {
  const ClassesTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}